pwd текущая директория

ls содержимое директории, ls пробел путь директории
-a скрытые
-h читаемые
-l столбец
можно писать подряд -ahl
скрытые файлы и папки с точки

man команда - справка или --help
q выход
clear очистить экран
exit

. текущая директория
.. директория на уровень выше
~ домашняя директория
* любое кол-во любых символов
? один любой символ

curl - вместо пинга

mkdir создать директорию, с ключом -p путь до директории

touch создать файл

cd войти в директорию
rm удалить файл, директорию -f без вопросов
cp путь1 путь 2
mv переместить

ctrl + c прервать
ctrl + z приостановить fg, bg вернуть

chmod права файл
chmod +x файл

cat файл - вывод содержимого на экран
less
nano

ввод и вывод
программа < файл - брать stdin из файла
программа > файл - выводить stdout в файл
программа >> файл - выводить stdout в файл с дозаписью
программа 2> файл - выводить stderr в файл
программа 2>> файл - выводить stderr в файл с дозаписью

конфеер pipe Прогр1 | Прогр2 |..| ПрогрN

wget ссылка - скачать файл по ссылке в текущую директорию
wget -P ссылка - скачать файл по ссылке в текущую директорию
wget -O ссылка - скачать файл по ссылке в текущую директорию задав имя файла
wget -c - докачать файл
wget  --spider - проверить доступность
wget -i текстовый файл - скачать несколько файлов по ссылкам из текстового файла
wget -r -l глубина ссылка - рекурсивное скачивание на указ глубину
-A тип файлов

unzip архиф.zip распаковать архив
gunzip
zip архив.zip файл1 файл2 ... - запаковать файлы в архив
gzip файл - запаковать файл в файл.gz, исходный удалить
tar -cvf архив.tar файл файл2 ... - запаковать файлы в tar без сжатия
tar -zcvf архив.tar.gz файл файл2 ... - запаковать со сжатием
tar -xvf архив.tar.gz файл файл2 ... - распаковка (z)
bzip2 (j)

find <папка> -name "<имя файла>" - найти файл в директории
grep "<строка>" <файл> - найти строку в файле
grep -c "<строка>" <файл> - кол-во строк
grep -r "<строка>" <папка> - найти строку во всех файлах папки

ssh логин@адрес_сервера -p порт

ключи доступа
на клиенте ~/.ssh/id_rsa ~/.ssh/id_rsa.pub - закрытый и публичный
на сервере ~/.ssh/authorized_keys
sudo apt-get install openssh-server
sudo apt-get purge openssh-server

ssh-keygen - создать ключи
ssh-add - сообщить системе ключи
cat ~/.id_rsa.pub - просмотр публичного ключа
nano ~/.ssh/authorized_keys - редактирование ключей

scp -P порт
	логин@адрес_сервера:путь1 путь2 - с сервера на клиент
scp -P порт
	путь1 логин@адрес_сервера:путь2 - с клиента на сервер
